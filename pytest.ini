[pytest]
testpaths = tests
addopts = -v --tb=short --cov=src --cov-report=term-missing --cov-report=html:tests/coverage_html
markers =
    unit: fast tests with no model loading
    integration: tests that load models or data from disk
filterwarnings =
    ignore::UserWarning
    ignore::FutureWarning
